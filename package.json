{
  "name": "fnb-tracker",
  "private": true,
  "version": "4.0.0",
  "description": "FnB Daily Tracker — Telegram Bot + Multi-Salesperson Dashboard (monorepo root)",
  "scripts": {
    "install:all": "npm install --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}
